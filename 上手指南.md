# QC Smart Reader 从零到能用

这份文档只回答一件事：**在你自己的 Mac 上，怎么把它真正跑起来。**
按顺序做完，大约 10 分钟。不需要部署远程服务；抓取网页、自动获取 YouTube 字幕或使用在线模型时仍需联网。

---

## 0. 先确认两件事

```bash
python3 -V     # 任意 3.9 以上都行
```

Chrome 已安装即可（Edge/Brave 等 Chromium 内核也行，加载方式相同）。

可选能力还有额外前置：扫描版 PDF OCR 需要 macOS 的 Swift + PDFKit/Vision 框架；YouTube 公开字幕自动获取需要 `yt-dlp` 已在 PATH 中；Codex 路线需要已登录的 `codex` CLI。缺少这些不影响其他能力。

> 之前跑不起来的原因就在这一步：旧版 `server.py` 里有 4 处写法只有 Python 3.12+ 能解析，
> 在 macOS 自带的 python3 上会直接 `SyntaxError` 退出，服务根本没起来。现在已修复，3.9 起都能跑。

---

## 1. 安装或启动本地服务

### 正式使用：安装后台服务（推荐）

解压 `qc-smart-reader-companion-<版本>.zip`，在解压后的目录运行：

```bash
bash install.command
```

安装器会校验 Companion 包、创建独立运行环境、安装并核对精确依赖、在升级前验证 SQLite 备份，再安装当前用户的 LaunchAgent。成功后服务会随登录自动启动，不需要一直留着终端窗口。安装器只有在服务身份、版本、数据目录、token 文件和受保护 API 全部验证通过后，才会显示并复制 Pairing Token；升级失败会恢复上一版运行环境与数据库备份。

以后升级时，解压新版 Companion 包后再次运行同一个 `install.command` 即可。

### 源码开发：临时启动

在仓库目录下：

```bash
bash start.command
```

（也可以在访达里直接双击 `start.command`。）

脚本会自动挑一个可用的 Python、建好 `.venv`、装上 `pypdf`，然后启动服务并打印：

```
[4/4] Service is up. Paste these two values into the extension side panel → 设置:
─────────────────────────────────────────────────────────────
  Companion URL   http://127.0.0.1:37621
  Pairing Token   xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
─────────────────────────────────────────────────────────────
  Vault           /Users/你/Documents/QC Smart Reader Vault/vault
  [✓] Pairing Token copied to clipboard.
```

在 macOS 上找到 `pbcopy` 时，脚本会自动复制 token 并明确提示。如果目标端口上已经是 QC Smart Reader，脚本会安全复用它、重新打印 token/Vault，然后退出；如果是其他程序，会明确拒绝，不会误杀该进程。显式传入 `--data-dir` 时，只会复用数据目录一致的已运行服务。

用 `start.command` 新启动服务时，**这个窗口要一直开着**；Control-C 会停止本次启动的服务。如果脚本报告“已复用”，要保持原来承载服务的窗口/进程运行。正式安装的 LaunchAgent 不需要这个窗口。

卸载后台运行环境但保留 Vault：

```bash
bash uninstall.command
```

只有明确执行 `bash uninstall.command --purge-data` 并逐字输入确认短语，才会永久删除整个 Vault。默认卸载永远保留研究数据。

---

## 2. 装扩展

1. Chrome 打开 `chrome://extensions`
2. 右上角打开**开发者模式**
3. 点**加载已解压的扩展程序**，选中这个仓库的根目录（有 `manifest.json` 的那层）
4. 点工具栏上的扩展图标 → 侧边栏打开

---

## 3. 配对（唯一容易卡住的一步）

侧边栏顶部切到 **设置** 标签：

| 字段 | 填什么 |
| --- | --- |
| 本地服务地址 | `http://127.0.0.1:37621` |
| Pairing Token | 第 1 步终端里打印的那串 |

点 **测试本地服务**。这不只是请求无需认证的 `/health`，还会用当前 token 请求受保护的 `/v1/projects`；看到 `本地服务和 Pairing Token 均正常：/Users/.../vault` 才表示真正配对成功。成功后会回到**聊天**并显示 Quick Start；token 缺失或错误会直接显示认证失败，不再把“服务存活”误报成“配对成功”。以后重新打开侧边栏时也会先做同样的受保护鉴权，不会只凭服务存活就误判为已配对。

> 没保存 token 的话，所有 `/v1/*` 请求都会返回 `missing x-qc-pairing-token`，
> 表现就是「点了没反应」。token 也可以从 `~/Documents/QC Smart Reader Vault/state/pairing_token.txt` 直接复制。

---

## 4. 先用本地模板，再按需选模型

新安装默认选择**本地模板（零配置，不调用外部模型）**。建议先用它跑通第一条证据，再按需到**设置 → 模型设置**选择外部模型路线。

### A. 本地模板（默认）

不需要填写任何模型配置。Quick Start 会读取并保存当前页，再用确定性规则生成一条结构化草稿 claim 与来自原文 chunk 的 exact quote。它不是 AI 总结，claim 措辞是模板化草稿；quote 虽然来自真实原文，仍必须由你核对它是否支持 claim，并明确点击接受后才能进入 `reviewed`。

这条路线不把材料发送给外部模型，不需要模型数据同意。需要模型的 Agent 对话会被明确阻止，不会静默切换路线。

### B. Codex CLI（不用 API Key）

- 服务商选 **Codex CLI（本机，用 ChatGPT 订阅，免 API Key）**
- Base URL、API Key 都留空
- 模型名留空 = 用 Codex 默认模型；要指定就填（例如 `gpt-5-codex`）
- **Codex CLI 命令**留空会从 companion service 的 PATH 查找 `codex`；也可填完整路径与 `exec`
- **Codex 超时**可在界面设置 10–3600 秒，默认 300 秒
- 保存后点 **测试模型**
- 阅读显著的数据使用说明，并勾选“我同意把我主动提交的材料发送给所选模型”；同意记录带隐私说明版本，说明更新后需要重新确认

原理：companion service 直接在本机跑 `codex exec`，把提示词从 stdin 喂进去、从 `--output-last-message` 读结果。
前提是终端里 `codex --version` 能正常输出。

默认调用会在一次性工作目录和隔离的 `HOME` / `CODEX_HOME` 中运行，并使用 Codex 的受管权限配置，只允许文件感知工具读取这个空白工作目录和最小运行时路径；同时启用 `--ephemeral`、忽略用户 config 与仓库 rules、禁用 shell、web search、插件和子代理，并且不继承 shell 环境变量。真实 CLI 合约测试会检查请求没有 shell / 委派工具，并确认文件系统处于受限模式。页面内容仍是不可信输入，结果仍需证据复核，提示内容也会发送给 Codex 服务；自定义命令不能覆盖这套文件权限。

**注意**：Codex 每次调用是一个完整 agent 回合，比直连 API 慢不少（几十秒级）。
适合逐篇精读；**上百条批量采集不要用这条路**，用 C 或先用 A 验证流程。

### C. OpenAI 兼容 / Anthropic API Key

- 服务商选 OpenAI-compatible 或 Anthropic
- 填 Base URL、API Key、模型名

⚠️ ChatGPT Plus / Claude 订阅**不等于** API Key。走这条路需要对应服务商单独开通 API 计费与额度；具体价格和限额以服务商当前页面为准。

---

## 5. 走一遍完整链路

先完成第一条证据：

1. 打开任意一篇正文可读的文章 → 侧边栏**聊天** → **从当前页生成第一条证据**。
2. 等待当前页保存进 Vault、本地模板抽取完成，并查看并排显示的 claim 与 exact quote。
3. 对照原文判断 quote 是否真的支持 claim；选择**接受：原文支持**或**拒绝：原文不支持**。系统不会替你做判断。
4. 关闭并重新打开侧边栏，确认 Quick Start 仍显示已保存的证据与人工审阅状态。

继续完整研究链路：

1. **知识库**：查看来源及本次生成的 claims / entities / evidence / risks / tasks；每条 claim 的 quote 必须能在原文 chunk 里逐字命中，否则会被标成 `pending_validation`。
2. **审阅**：在 **Claim / Evidence 审阅** 区域继续接受或拒绝其他记录。
3. **专题包**：填标题 → **生成专题包**。
4. **交付**：切到**交付** → **生成交付物**（报告 / PPT 提纲 / 视频脚本 / 策略任务书）。
5. **自检**：设置里点 **Vault Doctor** 和 **重建 Lineage**，确认索引和证据链一致。

其他常用入口：

- **PDF / 论文导入**：默认勾选“扫描件自动 OCR”。服务会先读 PDF 文本层，仅在图片型或低文本页面尝试 macOS Vision OCR；可以取消勾选。OCR 失败时会明确提示，并保留已有文本层继续导入。
- **YouTube 字幕导入**：填写有效的 YouTube URL，字幕文本留空会通过 PATH 中现有的 `yt-dlp` 获取公开字幕；也可以继续手贴普通文本或带时间戳字幕。
- **重跑结构化抽取**：在来源详情点 **重跑结构化抽取**。重跑前会重新校验历史证据引用；同一来源的 claim、evidence、relation、assumption、risk、strategy idea 和 task 都会幂等复用，并保留仍然有效的人工审阅状态。对应的结构化分析 Markdown 也会稳定覆盖，不会每次重跑都新增一份。

数据全部落在 `~/Documents/QC Smart Reader Vault/`：`vault/` 是可直接用 Obsidian 打开的 Markdown，`state/` 保存 SQLite、配对信息、审阅状态、任务和 lineage 等结构化状态。**不要只备份 Markdown，也不要把删除 SQLite 当成无损重建**；删除它会丢失结构化状态和关联关系。迁移或备份时请保留整个 `QC Smart Reader Vault/`，至少同时包含 `vault/` 与 `state/`。

---

## 6. 一条命令完整验收

在仓库根目录运行：

```bash
bash scripts/test_all.sh
```

也可以从其他目录用脚本的绝对路径调用；脚本会自行定位仓库，不依赖当前工作目录。它不会安装依赖，会自动选择 Python 3.9+ 和项目可用的 Node，依次执行 Python / JavaScript 语法检查、Python 单元测试、`start.command` 集成测试、macOS 安装/升级/回滚/卸载生命周期测试、全部 `.mjs` 测试（每个文件只跑一次），最后运行临时 vault 的端到端 smoke。

浏览器测试默认是严格模式：会真的拉起可用 Chromium 并加载扩展；缺 Playwright、缺可用浏览器或浏览器启动失败都会让验收失败，而不是静默 skip。最后会打印测试文件与 browser skip 计数，任一步失败都返回非零状态。

正式打包前，先确保所有发布输入已经提交且工作区干净，再运行：

```bash
python3 scripts/release.py --check
python3 scripts/release.py
```

发布器只会把明确 allowlist 中的扩展与 Companion 文件分别打入两个 ZIP，固定文件顺序、时间戳和权限，并生成 `SHA256SUMS`。任何缺失、未跟踪或已修改的发布输入都会阻止正式构建，避免把 `.venv`、测试数据或半成品误装进发行包。

---

## 7. 报错对照表

| 看到什么 | 什么意思 | 怎么办 |
| --- | --- | --- |
| `SyntaxError: f-string expression part cannot include a backslash` | 你跑的是旧副本 | 用本仓库当前版本，或 `git pull` |
| `Existing QC Smart Reader service found; reusing it.` | 同一端口已有可验证的 QC 服务 | 正常复用；脚本会重新打印配对信息并复制 token |
| `Port 37621 is already in use, but ... is not QC Smart Reader` | 端口被其他程序占用 | 停掉该程序，或给 `start.command` 传另一个 `--port`；脚本不会误杀它 |
| `QC Smart Reader ... uses data dir ... not requested ...` | 已运行服务与显式 `--data-dir` 不一致 | 停掉旧服务，或换端口启动所需数据目录 |
| `missing x-qc-pairing-token` | 扩展没填 token | 回第 3 步 |
| `invalid pairing token` | token 过期或复制错 | 重新从 `state/pairing_token.txt` 复制全串（别带换行） |
| `版本不兼容` / `扩展版本过旧` | 扩展和 Companion 不是同一代兼容版本 | 使用同一个 release 中的扩展 ZIP 与 Companion ZIP，重新安装 Companion 并重新加载扩展 |
| `origin not allowed` | 从普通网页而不是扩展发的请求 | 只从侧边栏操作；服务只放行 `chrome-extension://` 和 localhost |
| `Could not install the locked Python requirements` / `pypdf is unavailable` | 首次安装时网络不可用，或旧 `.venv` 与锁定版本不一致 | 恢复网络后重跑 `bash start.command`；仍失败时删除仓库内 `.venv` 后重跑。启动器会安装并校验 `requirements.txt` 的精确版本，不再静默进入缺少 PDF 功能的降级状态 |
| `OCR 失败，已回退到可用文本` | Vision OCR 不可用或识别失败 | 导入仍保留 PDF 文本层；确认是 macOS 且 Swift、PDFKit/Vision 可用，或关闭 OCR |
| `yt-dlp was not found on PATH` | 自动字幕依赖不存在 | 安装或让现有 `yt-dlp` 进入服务 PATH；也可直接粘贴字幕 |
| `No public subtitles were found ...` | 视频没有当前可读取的公开字幕 | 手工粘贴字幕；本项目不会登录、读 cookie 或转录音频 |
| `codex CLI was not found` | PATH 里找不到 codex | 终端确认 `codex --version`；或在模型设置的 **Codex CLI 命令** 填完整路径 |
| `codex CLI timed out after 300s` | 单次 Codex 回合太久 | 在模型设置调大 **Codex 超时**，或改用 API Key 路线 |
| `model API key is not configured` | 选了 API 路线但没填 key | 填 key，或把服务商换成 Codex CLI / 本地模板 |
| claim 全是 `pending_validation` | quote 在原文里对不上 | 正常的保护机制：重新采集该来源，或在审阅区手动改 quote |

---

## 8. 当前边界

- PDF OCR 使用 macOS Vision，只在 macOS 且 Swift 工具链、PDFKit/Vision 框架可用时工作；不可用或失败时会回退到 PDF 文本层，不会凭空补出扫描页内容。
- 本地 PDF 默认只允许从 Desktop、Documents、Downloads 和当前数据目录导入；外置盘或其他目录需用 `--allow-pdf-dir` 显式授权。远程 PDF 只接受公共 HTTP(S) 地址，会逐跳验证重定向并拒绝本机、内网、链路本地与保留地址。
- YouTube 自动模式只获取无需登录即可访问的公开字幕，不读取浏览器 cookie、不使用账号绕过限制，也不下载或转录音频；字幕下载同样拒绝本机、内网和重定向越界。没有公开字幕时仍需手工粘贴。
- `reextract` 会重校验证据，并对同一来源的全部结构化记录做幂等复用；模型本轮没有再次输出某条旧记录，并不等于该记录自动失效，删除或替换仍需人工判断。
- Codex 每次是完整 agent 回合，批量处理会明显变慢；大批量任务优先用 API 路线或先以 mock 验证流程。
