# QC Smart Reader 从零到能用

这份文档只回答一件事：**在你自己的 Mac 上，怎么把它真正跑起来。**
按顺序做完，大约 10 分钟。全程本地，不需要服务器，不需要联网部署。

---

## 0. 先确认两件事

```bash
python3 -V     # 任意 3.9 以上都行
```

Chrome 已安装即可（Edge/Brave 等 Chromium 内核也行，加载方式相同）。

> 之前跑不起来的原因就在这一步：旧版 `server.py` 里有 4 处写法只有 Python 3.12+ 能解析，
> 在 macOS 自带的 python3 上会直接 `SyntaxError` 退出，服务根本没起来。现在已修复，3.9 起都能跑。

---

## 1. 启动本地服务

在仓库目录下：

```bash
bash start.command
```

（也可以在访达里直接双击 `start.command`。）

脚本会自动挑一个可用的 Python、建好 `.venv`、装上 `pypdf`，然后启动服务并打印：

```
[4/4] Service is up. Paste these two values into the extension side panel → 设置:
─────────────────────────────────────────────────────────────
  Companion URL   http://127.0.0.1:37621
  Pairing Token   xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
─────────────────────────────────────────────────────────────
  Vault           /Users/你/Documents/QC Smart Reader Vault/vault
```

**这个窗口要一直开着**，关掉就等于关掉数据库。Control-C 停止服务。

---

## 2. 装扩展

1. Chrome 打开 `chrome://extensions`
2. 右上角打开**开发者模式**
3. 点**加载已解压的扩展程序**，选中这个仓库的根目录（有 `manifest.json` 的那层）
4. 点工具栏上的扩展图标 → 侧边栏打开

---

## 3. 配对（唯一容易卡住的一步）

侧边栏顶部切到 **设置** 标签：

| 字段 | 填什么 |
| --- | --- |
| 本地服务地址 | `http://127.0.0.1:37621` |
| Pairing Token | 第 1 步终端里打印的那串 |

点 **测试本地服务**，看到 `本地服务正常：/Users/.../vault` 就通了。

> 没保存 token 的话，所有 `/v1/*` 请求都会返回 `missing x-qc-pairing-token`，
> 表现就是「点了没反应」。token 也可以从 `~/Documents/QC Smart Reader Vault/state/pairing_token.txt` 直接复制。

---

## 4. 选一条模型路线

同样在**设置 → 模型设置**里选，三条路都能用：

### A. Codex CLI（推荐给你：不用 API Key，走你已买的 ChatGPT 订阅）

- 服务商选 **Codex CLI（本机，用 ChatGPT 订阅，免 API Key）**
- Base URL、API Key 都留空
- 模型名留空 = 用 Codex 默认模型；要指定就填（例如 `gpt-5-codex`）
- 保存后点 **测试模型**

原理：companion service 直接在本机跑 `codex exec`，把提示词从 stdin 喂进去、从 `--output-last-message` 读结果。
前提是终端里 `codex --version` 能正常输出。如果 `codex` 不在 PATH 里，可以在模型设置里覆盖启动命令
（`codex_command`，支持写完整路径）。

**注意**：Codex 每次调用是一个完整 agent 回合，比直连 API 慢不少（几十秒级）。
适合逐篇精读；**上百条批量采集不要用这条路**，用 B 或先用 C 验证流程。

### B. OpenAI 兼容 / Anthropic API Key

- 服务商选 OpenAI-compatible 或 Anthropic
- 填 Base URL、API Key、模型名

⚠️ ChatGPT Plus / Claude 订阅**不等于** API Key。走这条路需要单独的 API 额度（充值 $5 就够跑很久）。

### C. 先不配模型

什么都不填也能用：抽取会自动回退到内置 mock 模式，产出结构完整、引用真实（quote 确实来自原文 chunk）的记录。
用来验证链路和熟悉界面最合适，只是 claim 的措辞是模板化的。

---

## 5. 走一遍完整链路

1. **采集**：打开任意一篇文章 → 侧边栏 **聊天** 标签 → **读取当前页**
2. **抽取**：切到 **知识库** 标签 → **抽取结构化知识**
   - 出来的是 claims / entities / evidence / risks / tasks
   - 每条 claim 都挂着 quote，quote 必须能在原文 chunk 里逐字命中，否则会被标成 `pending_validation`
3. **审阅**：同一页往下，**Claim / Evidence 审阅** 区域，接受或拒绝
4. **专题包**：填标题 → **生成专题包**
5. **交付**：切到 **交付** 标签 → **生成交付物**（报告 / PPT 提纲 / 视频脚本 / 策略任务书）
6. **自检**：设置标签里点 **Vault Doctor** 和 **重建 Lineage**，确认索引和证据链一致

产物全部落在 `~/Documents/QC Smart Reader Vault/`：`vault/` 是纯 Markdown（Obsidian 直接能打开），
`state/` 是 SQLite 索引。删掉 SQLite 也不会丢内容，Markdown 才是底稿。

---

## 6. 一条命令自检

怀疑哪里坏了，先跑这个（会自己起一个临时服务和临时 vault，跑完自动清掉）：

```bash
python3 scripts/smoke_e2e.py
```

全绿就说明采集 → 分块 → 抽取 → claim/evidence → 专题包 → 交付物 → vault doctor → lineage 整条链路是通的，
问题一定出在扩展侧（多半是 token 或没选对项目）。

完整回归测试：

```bash
python3 -m unittest discover -s tests
node --test tests/test_browser_site_profiles.mjs tests/test_live_extension_capture_smoke.mjs \
  tests/test_quantclass_extractor.mjs tests/test_site_profiles.mjs \
  tests/test_sidepanel_strategy_tickets.mjs tests/test_sidepanel_project_dashboard.mjs \
  tests/test_sidepanel_batch_dispatch.mjs
```

（Node 那组会真的拉起 Chromium 加载扩展跑批量采集，需要本机装了 Playwright。）

---

## 7. 报错对照表

| 看到什么 | 什么意思 | 怎么办 |
| --- | --- | --- |
| `SyntaxError: f-string expression part cannot include a backslash` | 你跑的是旧副本 | 用本仓库当前版本，或 `git pull` |
| `Port 37621 is already in use` | 服务已经开着 | 浏览器打开 `http://127.0.0.1:37621/health` 确认；或关掉旧终端窗口 |
| `missing x-qc-pairing-token` | 扩展没填 token | 回第 3 步 |
| `invalid pairing token` | token 过期或复制错 | 重新从 `state/pairing_token.txt` 复制全串（别带换行） |
| `origin not allowed` | 从普通网页而不是扩展发的请求 | 只从侧边栏操作；服务只放行 `chrome-extension://` 和 localhost |
| `pypdf is unavailable` | PDF 依赖没装好 | `.venv/bin/pip install pypdf`；不影响 PDF 以外的功能 |
| `codex CLI was not found` | PATH 里找不到 codex | 终端确认 `codex --version`；或在模型设置里把 `codex_command` 写成完整路径 |
| `codex CLI timed out after 300s` | 单次 Codex 回合太久 | 调大 `codex_timeout_seconds`，或改用 API Key 路线 |
| `model API key is not configured` | 选了 API 路线但没填 key | 填 key，或把服务商换成 Codex CLI / 留空走 mock |
| claim 全是 `pending_validation` | quote 在原文里对不上 | 正常的保护机制：重新采集该来源，或在审阅区手动改 quote |

---

## 8. 现在还缺什么

不影响日常使用，但确实还没做：

- 扫描版 PDF 没有 OCR，只会被标成低置信度
- YouTube 需要你自己把字幕文本贴进去
- `reextract` 会重跑抽取，但不会自动重校验全部历史 claim
- 批量跑 Codex 路线会很慢（见第 4 步 A）
